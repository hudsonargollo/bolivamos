import MapClient from "./map-client";

export const metadata = {
  title: "Santa Cruz Map — BoliVibes",
  description: "Navigate Santa Cruz de la Sierra with BoliVibes featured places, venues, and city highlights.",
};

export default function MapPage() {
  return <MapClient />;
}
