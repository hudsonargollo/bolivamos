// Plain constant, not a Server Action — "use server" files may only export
// async functions, so this can't live in settings.ts alongside setBolipassPrice.
export const BOLIPASS_PRICE_KEY = "bolipass_price_bob";
