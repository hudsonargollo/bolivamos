// Placeholder — regenerate with `pnpm --filter @bolivibes/cron-worker run cf-typegen`
// once the bindings below are created and wired into wrangler.jsonc.
interface CloudflareEnv {
  DB: D1Database;
  BOLIVIBES_KV: KVNamespace;
  RESEND_API_KEY: string;
}
