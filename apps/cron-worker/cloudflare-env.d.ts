// Placeholder — regenerate with `pnpm --filter @bolivamos/cron-worker run cf-typegen`
// once the bindings below are created and wired into wrangler.jsonc.
interface CloudflareEnv {
  DB: D1Database;
  BOLIVAMOS_KV: KVNamespace;
  RESEND_API_KEY: string;
}
