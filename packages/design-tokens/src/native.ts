import { colors, fontSizes, radii, spacing } from "./tokens";

/**
 * Plain-value tokens for React Native `StyleSheet`/animated-value use where
 * NativeWind className strings aren't convenient (chart colors, Reanimated
 * interpolations, etc). Mirrors the same source values as tailwind-preset.ts.
 */
export const nativeColors = colors;
export const nativeFontSizes = fontSizes;
export const nativeSpacing = spacing;
export const nativeRadii = radii;
